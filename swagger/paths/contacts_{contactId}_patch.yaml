patch:
  tags:
    - Contacts
  summary: Оновити контакт за ідентифікатором
  operationId: updateContactById
  description: Оновлює дані контакту за його унікальним ідентифікатором. Потрібна авторизація через Bearer токен.
  security:
    - bearerAuth: []
  parameters:
    - name: contactId
      in: path
      required: true
      description: Унікальний ідентифікатор контакту
      schema:
        type: string
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../../components/contactUpdate.yaml'
  responses:
    '200':
      description: Контакт успішно оновлено
      content:
        application/json:
          schema:
            $ref: '../../components/contact.yaml'
    '400':
      description: Некоректні дані
      content:
        application/json:
          schema:
            $ref: '../../components/error.yaml'
    '404':
      description: Контакт не знайдено
      content:
        application/json:
          schema:
            $ref: '../../components/error.yaml' 